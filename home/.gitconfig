[alias]
	st = status
	r = rebase -i origin/master
	pr = !git fetch && git rebase origin/master
	aa = commit --amend -a
	a = commit --amend
	rc = rebase --continue
[include]
    path = ~/.gitconfig.local
